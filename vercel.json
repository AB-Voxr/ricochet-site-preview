{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "mkdir -p out/products && cp index.html shop.html inside.html mission.html style.css robots.txt out/ && cp products/*.html out/products/ && cp -r img out/img",
  "installCommand": "echo skip",
  "outputDirectory": "out",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [
    { "source": "/", "destination": "https://ricochetsupplements.com/" },
    { "source": "/:path*", "destination": "https://ricochetsupplements.com/:path*" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [{ "key": "X-Robots-Tag", "value": "noindex, nofollow" }]
    }
  ]
}
